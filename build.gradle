plugins {
    id "java"
    id "application"
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

sourceSets {
    main {
        java {
            srcDirs = ['src']
        }
    }
}

dependencies {
    implementation files("./libs/sround-v_test.jar")
}

application {
    mainClass = 'org.lemon.Lemon'
}

tasks.register('runCustom', JavaExec) {
    mainClass = 'org.lemon.Lemon'
    classpath = sourceSets.main.runtimeClasspath
}